# Subagent API contract

Branch: `feature/<slug>--api`
Scope: API routes, server actions, service layer.

Rules:
- Work only on your agent branch.
- Watch Next.js server action conventions.
- Avoid schema changes; if needed, coordinate with DB agent.

Deliver:
- commit(s) on your agent branch
- notes about endpoints + tests
