# Subagent UI contract

Branch: `feature/<slug>--ui`
Scope: UI components, pages, client state.

Rules:
- Work only on your agent branch.
- Keep changes aligned with spec.md.
- Do not touch prisma/schema.prisma.
- Run `npm run lint` and relevant UI smoke if possible.

Deliver:
- commit(s) on your agent branch
- update your task json status to done
- write notes in logs/agent-ui.log
