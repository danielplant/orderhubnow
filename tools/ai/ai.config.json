{
  "project": "orderhubnow",
  "default_base_branch": "main",
  "feature_branch_prefix": "feature/",
  "worktree_root": "../worktrees",
  "default_worktree": true,
  "worktree_copy_env": true,
  "agent_branch_suffixes": [
    "ui",
    "db",
    "infra",
    "api"
  ],
  "run_dir": ".ai/runs",
  "current_run_pointer": ".ai/current-run",
  "commands": {
    "type_check": "npm run type-check",
    "lint": "npm run lint",
    "build": "npm run build",
    "schema_drift": "bash tools/ai/lib/check-schema-drift.sh",
    "apply_migration": "npm run apply-migration",
    "clear_next_cache": "rm -rf .next",
    "db_tunnel": "npm run db:tunnel",
    "test_dev": "npm run dev",
    "test_prod": "rm -rf .next && npx prisma generate && npm run build && npm start"
  },
  "migration": {
    "schema_file": "prisma/schema.prisma",
    "migration_dirs": [
      "prisma/migrations",
      "sql-migrations"
    ]
  },
  "merge": {
    "method": "merge",
    "no_ff": true
  },
  "pr": {
    "target": "main",
    "recommended_merge": "squash"
  }
}
